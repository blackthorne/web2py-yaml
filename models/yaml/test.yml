db:
  name: test
  dbms: postgres
  dbfile: storage.sqlite
  user: john
  pass: Doe
  hostname: localhost
  parameters: pool_size=10
  options: none

animal:
  name: string
  type: string
  sex: string
  extra: reference name
  
person:
  name: string
  age: int, requires=IS_INT(), default='12'
  color:
    type: string
    requires: IS_INT()
    default: IS_INT()